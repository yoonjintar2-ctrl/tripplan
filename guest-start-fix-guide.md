# 게스트 시작 버튼·로그인 오류 수정

첨부 화면의 `GuestDrafts is not defined`는 Google 인증 전에 사이트 내부에서 발생한 오류입니다. 게스트 저장 기능을 별도 파일의 전역 변수로 참조하고 있어, 그 파일이 누락되거나 로딩되지 않으면 게스트 시작과 로그인 준비가 함께 중단됐습니다.

이제 게스트 저장 기능을 app.js에 포함해 별도 스크립트의 로딩 여부·순서에 의존하지 않습니다. HTML도 새 버전 app.js를 요청하도록 변경했습니다. 기존 임시 계획 저장 키와 형식은 그대로입니다.

## 적용
1. ZIP을 풀어 내부 파일/폴더를 GitHub 저장소 최상위에 덮어씁니다. 내부 dist의 파일들은 기존 dist 안에 들어가야 합니다.
2. 이전 비로그인 업데이트 관련 파일도 함께 포함되어 있습니다. 일부 파일만 골라 올리지 말고 전체를 올리세요. 100개 미만입니다.
3. GitHub Actions 배포가 완료되면 원래 계획을 작성하던 주소에서 Ctrl+Shift+R로 새로고침합니다. 브라우저 데이터/로컬 저장소는 삭제하지 마세요.
4. 비로그인 상태에서 ‘내 계획 시작하기’ 또는 ‘여행 관리’를 누르고 여행·일정을 작성합니다.
5. Google 로그인 후 같은 사이트로 돌아왔을 때 작성한 계획이 계정에 저장되는지 확인합니다.

## 데이터 보존
로그인 이동 전에 계획과 작성 중인 입력을 브라우저에 보관합니다. 로그인 후 계정으로 옮기며, 저장 확인 전에는 로컬 백업을 지우지 않습니다. 일부 저장 실패 후 재시도해도 같은 계획이 중복 생성되지 않도록 처리합니다.

## 검증 범위
별도 guest-drafts.js와 전역 GuestDrafts가 없는 조건에서 실제 메뉴 클릭 → 여행 저장 → 일정 작성 → 모의 인증 복귀 → 계정 저장 및 작성 중 입력 복원 테스트를 통과했습니다. 전체 자동 테스트도 통과했습니다. 실제 Google 계정 인증·운영 데이터베이스 저장은 배포 후 확인해야 합니다.

이번 화면의 오류를 해결하기 위해 Supabase 설정을 새로 바꿀 필요는 없습니다. 다만 앞서 발생했던 예전 도메인으로 돌아가는 문제는 별개이므로, 그 증상이 남아 있다면 기존 login-fix-guide.md의 Site URL / Redirect URLs 설정도 확인하세요.

개발 참고: dist/guest-drafts.js는 소스/단위 테스트용으로 유지됩니다. 이를 수정하면 npm run build:guest로 app.js 안의 코드를 갱신합니다. npm test는 포함된 코드가 원본과 같은지도 확인합니다.
