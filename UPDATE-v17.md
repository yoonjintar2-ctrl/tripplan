# 여행계획닷컴 v17 수정본

## 적용 방법

이 파일은 기존 저장소에 덮어쓰는 업데이트입니다. 기존 `dist/assets`와 도메인 설정 파일은 그대로 유지하세요.

1. ZIP을 압축 해제합니다.
2. GitHub `yoonjintar2-ctrl/tripplan`의 `main` 브랜치 **저장소 최상위**에서 Add file → Upload files를 누릅니다.
3. 압축 해제한 폴더 안의 `dist`, `scripts`, `tests`, `supabase`, `package.json`, `UPDATE-v17.md`를 올립니다. ZIP 파일 자체를 올리지 마세요. `dist` 폴더 안으로 들어가서 `dist`를 다시 올리면 경로가 중복되므로 최상위에서 올려 주세요.
4. Commit changes 후 GitHub Actions 배포가 성공하면 https://trip.jintar.co.kr/ 를 새로고침합니다. 사이트 데이터나 쿠키를 삭제할 필요는 없습니다.

서버의 초대 권한·참여 계정 조회·날짜 변경 보존 처리는 현재 연결된 Supabase 프로젝트에 이미 적용하고 확인했습니다. 포함된 SQL은 변경 기록이며 별도로 실행할 필요가 없습니다. 이번 ZIP은 100개 미만으로, 한 번에 업로드할 수 있습니다.

## 반영 사항

- **초대 링크 편집:** 초대받기 전에는 여행을 읽을 수 없어서 초대 코드 검증도 실패하던 순환 권한 문제를 수정했습니다. 로그인한 계정과 올바른 초대 코드가 있어야 편집자로 참여합니다. 기존 열람 전용 계정이 같은 링크를 다시 열어도 권한이 임의로 올라가지 않습니다. 여행 소유자가 지정한 열람 전용 권한은 유지됩니다.
- **참여 계정:** 여행 정보에 로그인한 참여자의 이름·계정 이메일·수정 권한을 표시합니다. Google 로그인 및 초대 참여가 완료된 뒤 표시되며, 여행의 동행 캐릭터와 계정 권한은 구분합니다. 이메일 목록은 해당 여행 구성원만 조회할 수 있습니다.
- **여행 카드:** 불러오기·링크·복제하기·삭제하기를 같은 너비의 4칸으로 배치했습니다.
- **날짜 입력:** 일정 및 여행 날짜 입력 박스의 숫자·여백을 클릭해도 브라우저 날짜 선택기를 엽니다. showPicker를 지원하지 않는 브라우저는 기존 기본 날짜 입력을 유지합니다.
- **지명 글자:** 공원 지명의 글자와 테두리에 같은 진한 색을 적용하던 설정을 수정했습니다. 파스텔 지도와 밝은 글자 테두리를 유지합니다. Google 원본의 현지어/한국어 병기는 별개입니다.
- **서울 기본 위치:** 장소를 지정하지 않은 새 일정 지도는 서울 시청 주변에서 시작합니다. 검색 결과나 기존 일정의 실제 위치는 그대로 표시합니다.
- **일정 이름:** 좌표만 선택한 위치는 일정 이름을 비워 둡니다. 실제 장소 이름이 있을 때만 자동 입력하며, 사용자가 직접 적은 이름은 유지합니다.
- **날짜 변경 시 보존:** 여행의 시작일을 옮기면 기존 일차에 맞춰 일정을 이동합니다. 기간을 줄여 넘치는 일정은 마지막 날에 모으며 삭제하지 않습니다. 예를 들어 3일차 일정이 있는 여행을 2일로 줄이면 해당 일정은 2일차로 이동합니다. 게스트와 로그인한 여행 모두 적용했습니다. 이미 과거 버전에서 삭제된 일정이 자동 복구되는 기능은 아닙니다.
- **저장:** 앞의 0을 제거해 `0분 전에 저장`으로 표시합니다. 상단에 저장 버튼을 추가했습니다. 게스트에서는 현재 계획을 브라우저에 저장하고, 로그인 상태에서는 각 편집 작업이 즉시 저장되는 기존 방식에 더해 서버 저장 상태를 수동 확인합니다. 공동 편집 중인 다른 사람의 변경을 오래된 화면 데이터로 덮어쓰지 않습니다. 통신 실패는 성공으로 표시하지 않습니다. 작성 중인 일정/여행 정보는 해당 창의 저장 버튼으로 제출합니다.
- **리뷰:** 기존 3개 제한을 제거해 Google에서 반환한 리뷰를 모두 보여주고, 본문을 펼친 상태로 스크롤할 수 있습니다. Google API는 장소당 최대 5개만 제공하므로 무한 로딩 대신 하단에 Google 지도에서 전체 리뷰를 보는 링크를 제공합니다. https://developers.google.com/maps/documentation/places/web-service/reference/rest/v1/places

## 검증 범위

- 현재 서버에서 임시 여행을 이용해 정상 초대 참여·편집, 잘못된 코드 차단, 소유자 권한 상승 차단, 구성원 계정 조회 제한, 열람 전용 수정 차단을 검증했습니다. 테스트의 임시 데이터는 모두 롤백했습니다.
- 현재 서버에서 기간 단축·연장 후 일정 식별자·메모·금액이 유지됨을 확인했습니다. 구버전이 삭제 확인 값을 보내더라도 일정을 삭제하지 않습니다.
- DOM 통합 테스트에서 게스트 날짜 변경과 새로고침 보존, 날짜 박스 클릭, 서울 기본 위치, 실제 장소 이름/좌표 처리, 참여 계정 표시, 저장 상태와 통신 실패, 리뷰 5개 표시를 확인했습니다. 기존 게스트 로그인 전환·복제 등 회귀 테스트도 통과했습니다.
- DOM 테스트의 Google 지도·로그인 응답은 모의 처리했습니다. 이번 ZIP을 실제 배포한 뒤의 Google 로그인 화면 및 PC/모바일 지도 렌더링까지 확인한 것은 아닙니다.
