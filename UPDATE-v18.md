# 여행계획닷컴 v18 — 모바일 화면·선택 복원 수정

## 올리는 방법

ZIP을 압축 해제한 뒤, GitHub 저장소의 main 브랜치 최상위에서 내용물의 폴더 구조를 유지해 덮어 올려 주세요. `dist` 안에 다시 `dist`를 만들지 마세요. 기존 `dist/assets`, 도메인 설정 및 GitHub Actions 파일은 유지합니다. ZIP 자체를 업로드하는 것이 아니라 압축을 푼 내용물을 올립니다. 100개 미만 파일입니다.

GitHub Actions 배포가 완료되면 https://trip.jintar.co.kr/ 를 새로고침해 주세요. 사이트 데이터나 쿠키는 삭제하지 않아도 됩니다. v17 변경도 포함하며, 새로 실행해야 할 SQL은 없습니다. 포함한 v17 SQL은 이미 적용된 변경 기록입니다.

## 변경 내용

- 850px 이하 화면에서 여행 선택 드롭다운은 남는 너비만 사용하고, 여행관리 버튼은 오른쪽 82px 너비를 확보해 화면 밖으로 밀리지 않도록 했습니다.
- 모바일의 여행 영수증 진입 메뉴를 숨겼습니다. PC 영수증과 기존 정산 데이터는 유지합니다.
- 모바일 일정 추가 버튼은 여행관리 버튼 바로 아래, D-day와 같은 행 오른쪽에 작게 배치했습니다. 목록 아래의 기존 큰 버튼은 모바일에서 숨깁니다.
- 모바일 일정 카드는 제목 한 줄과 수정 버튼만 표시합니다. 카드를 누르면 해당 카드 안에 전체 제목·날짜·시간·분류·메모·설정된 금액을 표시합니다.
- 여행관리 팝업을 모바일에서 `여행 목록 / 여행 정보` 탭으로 분리했습니다. 목록의 여행을 고르거나 새 여행을 누르면 정보 탭으로 이동합니다. PC에서는 기존 두 영역을 함께 표시합니다.
- 여행 기간 밖의 날짜를 선택하면 여행관리에서 시작일·종료일을 수정하라는 알림을 띄우며, 저장 단계에서도 같은 검사를 수행해 기간 밖 일정을 저장하지 않습니다.
- 게스트와 로그인 계정별로 마지막 여행·날짜·일정을 저장하고 복원합니다. 여행을 바꿨다가 돌아와도 해당 여행의 선택을 복원합니다. 삭제된 일정이나 변경된 여행 기간에 맞지 않는 선택은 복원하지 않습니다.
- 브라우저 복귀 시 같은 계정의 SIGNED_IN 이벤트가 다시 발생해도 여행을 처음부터 불러오지 않습니다. 로그아웃·계정 변경 후에는 정상적으로 해당 계정의 여행을 불러옵니다.
- ON AIR 여행도 복원 직후에는 보던 선택을 유지합니다. 기존 실시간 따라가기 요구대로 이후 분 단위 갱신에서는 현재 일정으로 따라갑니다.

## 확인 결과

기존 전체 자동 테스트와 새 모바일 동작·선택 복원 테스트가 통과했습니다. 게스트 여행 전환/새로고침, 계정별 선택 분리, 동일 계정 로그인 재통지, 모바일 상세 펼치기/여행관리 탭/일정 추가, 기간 밖 일정 차단을 DOM 통합 테스트로 확인했습니다. Google Maps 및 Supabase 로그인 응답은 모의 처리했습니다. 실제 iPhone Safari에서 이 수정본을 배포 후 실행한 검증은 아닙니다.
