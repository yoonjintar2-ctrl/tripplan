# 여행계획닷컴 v22 누적 업데이트

## 설치

1. ZIP을 압축 해제합니다.
2. 내용물(dist, scripts, tests, supabase, package.json, 이 안내서)을 GitHub 저장소 최상위에 올려 기존 파일을 덮어씁니다. ZIP 자체를 올리거나 dist 안에 dist를 만들지 마세요.
3. 기존 dist/assets 폴더와 CNAME, GitHub Pages 설정은 유지하세요. 이 파일은 기존 사이트용 누적 업데이트이며 이미지 전체를 포함한 신규 설치본은 아닙니다.
4. GitHub Pages 배포가 완료되면 페이지를 새로고침합니다.

v17~v21의 코드 수정도 포함합니다. v22용 추가 SQL 작업은 없습니다. 기존 초대/여행 날짜 보존용 SQL도 누적본에 포함되어 있으나 이미 적용했다면 다시 실행할 필요가 없습니다.

## 이번 변경

- 모바일 일정의 펼치기 버튼을 ‘자세히’로 변경. 열면 ‘접기’로 표시합니다.
- 지도 이전/다음 버튼으로 선택한 일정이 일정 목록 안에 보이도록 자동 스크롤합니다. 페이지 전체를 스크롤하지 않습니다.
- 실제 조회된 Google 경로에 작은 흰색 시간 배지를 표시합니다: 도보 N분 / 차량 N분 / 대중 N분.
- 도보 30분 이내는 도보 우선. 그 이상은 차량 기본이며 지하철·철도가 포함된 대중교통이 차량보다 빠르면 대중교통을 선택합니다. 차량 조회가 불가능하고 대중교통만 가능하면 대중교통을 사용합니다.
- 캡틴은 경로의 실제 좌표를 따라 이동하며 도보/차량/지하철/버스에 맞추어 표현됩니다. 차량 등은 기존 캡틴 이미지와 이동수단 아이콘을 결합한 간단한 애니메이션입니다.
- 경로를 받지 못하면 경로선과 시간을 표시하지 않고 캡틴이 점프합니다.
- 일자만 선택하면 첫 일정부터 마지막 일정까지 순서대로 이동하고, 끝에서는 처음으로 점프하여 반복합니다.
- 첫날 첫 목적지에는 인천공항 좌표에서 출발하는 비행기 오프닝을 넣었습니다. 실제 항공편이나 항공 경로가 아닌 연출이며, 국내 목적지에도 같은 오프닝이 적용됩니다.
- 애니메이션은 수 초로 압축됩니다. 표시된 이동시간만 Google의 예상시간이며 조회 시점 기준입니다.
- 기기에서 동작 줄이기를 설정한 경우 캡틴 이동 애니메이션을 생략합니다.

## 실제 경로를 표시하기 위한 Google 설정

현재 사이트에서 쓰는 지도 API 키의 Google Cloud 프로젝트를 선택하세요.

1. Google Cloud 콘솔 → API 및 서비스 → 라이브러리 → Routes API → 사용 설정.
2. Maps JavaScript API도 사용 설정되어 있어야 하며 프로젝트에 결제 계정이 연결되어 있어야 합니다.
3. API 및 서비스 → 사용자 인증 정보 → 현재 지도 API 키에서 API 제한을 사용한다면 Routes API를 허용 목록에 추가합니다. 기존 Maps/Places 허용 항목과 웹사이트 제한은 유지합니다.
4. 적용 후 사이트를 새로고침하고 위치가 등록된 일정 두 개 사이에서 이동해 보세요.

[Google 공식 Routes 시작 안내](https://developers.google.com/maps/documentation/javascript/routes/start)

경로 조회는 API 사용량에 포함됩니다. 같은 구간은 페이지 내 메모리에 5분간 재사용하며, 이동수단 비교 때문에 한 구간에서 최대 세 가지 경로를 조회할 수 있습니다. 해당 지역의 경로가 없거나 API 권한/할당량 문제로 응답이 없으면 점프 처리가 정상입니다.

## 확인 범위

npm test 전체 통과. 경로 선택, 30분 경계, 지하철 비교, 조회 실패 시 점프, 경로 보간, 선택 이동과 도착 처리는 Google 응답을 모의한 자동 테스트로 확인했습니다. 실제 Cloud 프로젝트의 Routes API 활성화 여부, 실제 경로 응답, 실제 아이폰 Safari 화면은 이번 작업에서 검증하지 못했습니다.
