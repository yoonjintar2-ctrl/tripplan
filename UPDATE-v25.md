# 여행계획닷컴 v25 누적 업데이트

## 적용 방법

1. ZIP을 풀고 내용물을 기존 GitHub 저장소 최상위에 덮어쓰세요. ZIP 자체를 올리거나 dist 안에 dist를 만들지 마세요.
2. 기존 이미지 폴더, CNAME, GitHub Pages 설정은 유지하세요. 기존 사이트용 누적 업데이트이며 v17 이후 코드와 v23 차량·비행기 이미지가 포함됩니다.
3. Supabase SQL Editor에서 `supabase/migrations/20260921233515_transit_transport_mode.sql` 내용을 한 번 실행하세요. 기존 연결 프로젝트의 v24까지 설정은 이미 적용되어 있지만, 이번 TRANSIT 허용 변경은 연결 도구의 HTTP 400 / Invalid MCP request metadata 오류로 직접 적용하지 못했습니다. GitHub에 SQL 파일을 업로드하는 것만으로 DB에는 적용되지 않습니다. 이 단계 전에는 로그인 계정의 대중교통 선택 저장이 실패할 수 있습니다. 기존 계획은 삭제하지 않습니다.
4. GitHub Pages 배포 완료 후 새로고침하세요.

## 변경 사항

- 모바일 장소 정보창: 낮고 가로로 길게 배치하고 왼쪽 사진 1.8장, 오른쪽 리뷰를 표시합니다. 사진은 가로, 리뷰는 내부 세로 스크롤로 확인합니다.
- 일자의 마지막 일정에서 다음을 누르면 검은 지도 화면에 ‘다음날’을 표시한 뒤 다음 날짜 첫 일정으로 이동합니다. 첫 일정에서 이전을 누르면 ‘하루 전’과 함께 이전 날짜 마지막 일정으로 이동합니다. 빈 날도 날짜 순서대로 방문하며 여행 기간 밖으로는 넘어가지 않습니다.
- 모바일 제목과 설명 사이의 불필요한 빈 공간을 줄였습니다.
- 일정 등록·수정 입력칸과 시간 버튼의 모서리를 10px로 통일했습니다.
- 지도 이동 버튼은 ‘이전 일정’과 ‘다음 일정’ 원형 버튼 두 개로 분리하고 일정 개수 표시는 없앴습니다.
- 이동수단은 도보 → 차량 → 대중 → 기타 → 도보 순환입니다. Google이 대중교통 경로를 반환하지 않으면 기타로 건너뜁니다. 기타는 시간 없이 점선 직선과 걷기 모션을 표시합니다. 선택은 게스트 계획 및 계정에 저장됩니다(위 SQL 적용 필요).
- 모바일 검색 장소의 자세히 버튼을 닫기 오른쪽으로 옮기고 검색 실행 버튼은 짙은 배경과 흰 글씨로 변경했습니다.
- PC·모바일의 일자 버튼을 누르면 해당 날짜의 좌표가 있는 모든 일정이 보이도록 지도 범위를 맞춥니다. 개별 일정 선택은 확대율을 유지하며 해당 위치로 이동합니다.

## 검증 범위

자동 테스트로 날짜 경계 이동, 빈 날짜, 여행 기간 경계, 일자 선택 시 범위 조정, 개별 선택 시 재확대 방지, 검색 자세히 버튼 위치, 대중교통 순환과 조회 불가 시 건너뛰기, 기존 게스트·로그인 저장 동작을 확인했습니다. 지도 API는 모의 응답으로 검증했습니다. 실제 iPhone Safari 화면, 실시간 Google 경로 응답, 이번 DB 변경은 검증하지 못했습니다. 실제 사이트에 배포한 파일이 아니라 업로드용 수정본입니다.
