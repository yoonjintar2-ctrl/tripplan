# 로그인 복귀 주소 수정 및 샘플 여행 업데이트

## 이번 수정
- Google 동의 화면을 매번 강제 요청하던 옵션을 제거했습니다.
- Supabase 로그인 정보 보관 및 토큰 자동 갱신을 명시했습니다. 기존 저장 키는 변경하지 않았습니다.
- 인증 처리 후 URL의 인증 코드/오류 정보를 정리하고, 실패하면 재시도 안내를 표시합니다.
- 비로그인 첫 방문부터 여행 선택 목록에 ‘서울 하루 산책 · 샘플 여행’이 나옵니다. 4개 일정·지도 위치·예시 비용이 있으며 읽기 전용입니다. 다른 여행을 선택하면 원래 계획으로 돌아옵니다. 샘플은 계정에 자동 저장되지 않습니다.
- 기존 임시 계획 보관 및 로그인 후 계정 저장 처리를 유지했습니다.

## 반드시 함께 확인할 Supabase 설정
첨부 화면은 로그인 후 예전 morrow-trip-planner.yoonjintar0.chatgpt.site 주소로 돌아간 상태입니다. 현재 사이트와 다른 주소로 돌아가면 로그인 검증에 필요한 브라우저 저장소를 이어받을 수 없습니다. 코드에 지정한 복귀 주소가 허용 목록에 없는지, Site URL이 예전 주소인지 확인해야 합니다. 서버 설정에 직접 접근하지 않았으므로 원인은 설정 화면에서 최종 확인해야 합니다.

1. Supabase에서 현재 앱이 연결한 프로젝트를 엽니다. 프로젝트 ref는 jiaqobfriamuxtvxhrls이며 표시 이름은 아닙니다.
2. Authentication → URL Configuration에서 Site URL을 현재 실제 운영 주소로 설정합니다. HTTPS, 경로, 마지막 /까지 확인하세요.
3. Redirect URLs에도 같은 주소를 추가합니다. 아래 trip.jintar.co.kr은 앞서 제안했던 예시입니다. 실제 선택한 하위 도메인이 다르면 반드시 바꾸세요.

   Site URL 예시: https://trip.jintar.co.kr/

   Redirect URLs 예시(각각 한 줄):
   ```text
   https://trip.jintar.co.kr/
   https://trip.jintar.co.kr/\?*
   ```
   두 번째는 공유 여행의 trip/invite 쿼리를 포함한 복귀를 허용하는 패턴입니다. `\?`는 물음표 자체를 뜻합니다. 도메인 전체를 허용하는 범용 와일드카드는 필요하지 않습니다.

   아직 GitHub 기본 주소로 사용하는 경우에는 대신 다음 주소/패턴을 등록합니다.
   ```text
   https://yoonjintar2-ctrl.github.io/tripplan/
   https://yoonjintar2-ctrl.github.io/tripplan/\?*
   ```
4. 예전 chatgpt.site 주소가 더 이상 사용되지 않는다면 허용 목록에서 제거합니다. 새 운영 주소를 먼저 등록하세요.
5. Google Cloud의 OAuth 클라이언트 ‘승인된 리디렉션 URI’는 다음 Supabase 콜백이어야 합니다. 여기에 사이트 주소를 대신 넣지 마세요.
   ```text
   https://jiaqobfriamuxtvxhrls.supabase.co/auth/v1/callback
   ```
6. 저장한 뒤 예전 주소의 로그인 결과 화면을 재사용하지 말고, 계획을 작성하던 새 사이트 주소로 돌아가 Google 로그인을 다시 시작하세요. 인증 코드가 포함된 전체 URL은 공유하지 마세요.

## 업로드
tripplan-login-sample-update.zip은 직전 비로그인 업데이트가 적용된 저장소에 덮어쓰는 수정본입니다. ZIP을 풀고 내부 dist 폴더가 저장소의 dist 폴더와 겹치도록 업로드하세요. dist 안에 dist가 한 번 더 생기지 않게 주의하세요. GitHub Actions 배포 완료 후 확인합니다. SQL 실행은 필요하지 않습니다.

## 확인 순서
1. 현재 운영 주소에서 비로그인 계획을 하나 작성합니다.
2. 샘플 여행을 선택했다가 원래 여행으로 돌아왔을 때 계획이 그대로인지 확인합니다.
3. Google 로그인 후 같은 운영 주소로 돌아오고 계정에 계획이 저장되는지 확인합니다.
4. 탭을 닫았다가 같은 브라우저·같은 HTTPS 주소로 다시 열어 로그인 상태를 확인합니다.
5. 공유 링크에서 로그인한 뒤 해당 여행으로 돌아오는지도 확인합니다.

일반 브라우저에서는 저장된 세션을 재사용합니다. 시크릿 창 종료, 브라우저 데이터 삭제, 명시적인 로그아웃, 서버 세션 만료 정책에는 다시 로그인이 필요할 수 있습니다. 서로 다른 도메인 간 로그인/임시 계획은 자동 공유되지 않습니다. 이번 작업에서는 모의 인증으로 자동 테스트를 통과했으며, 실제 Google 계정 로그인 및 Supabase 운영 설정 변경은 수행하지 않았습니다.

공식 참고: https://supabase.com/docs/guides/auth/redirect-urls
