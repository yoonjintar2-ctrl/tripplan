# 여행계획닷컴 v24 누적 업데이트

## 적용

ZIP을 풀어 내용물을 GitHub 저장소 최상위에 덮어쓰세요. dist 안에 dist를 만들지 마세요. 기존 이미지 폴더, CNAME, GitHub Pages 설정은 유지합니다. v17~v23 수정과 런웨이 차량·비행기 파일도 포함한 기존 사이트용 누적본입니다.

현재 연결된 Supabase에는 기타 이동수단 저장 설정까지 적용했습니다. 같은 프로젝트라면 SQL을 별도로 실행할 필요 없습니다. 다른 프로젝트에 적용할 때는 포함된 마이그레이션을 날짜 순으로 적용하세요.

## 변경

- 걷기 발동작 주기: 0.4초 → 0.85초. 이동 애니메이션도 도보 구간 기준 약 3.2~6.5초로 늦췄습니다.
- 경로를 찾지 못하면 시간 없이 점선 직선을 표시하고 캡틴이 그 선을 따라 걷습니다. 직선은 실제 도로·보행 경로 안내가 아닙니다.
- 이동수단 버튼은 도보 → 차량 → 기타 이동수단 → 도보 순서입니다. 기타에는 시간을 표시하지 않으며 실제 경로 조회 없이 직선 점선을 사용합니다. 대중교통 추천 버튼에서 누르면 먼저 도보로 바뀝니다.
- 선택한 이동수단은 게스트 임시 계획과 로그인 계정에 저장됩니다. 저장 실패 시에는 이전 선택을 유지합니다.
- 지도 클릭 카드의 ‘일정 추가’ 아래에 사진·리뷰 미리보기와 ‘자세히’를 추가했습니다.
- 일정 선택 시 좌측 하단 장소 정보에도 ‘자세히’를 추가했습니다.
- 자세히는 사이트 내 팝업으로 열립니다. Google이 제공하는 사진·평점·리뷰·주소·영업시간·전화·홈페이지를 표시하고 전체 Google 지도 정보 링크를 제공합니다.
- 빈 좌표를 클릭했거나 장소 ID가 없는 일정은 다른 상호를 추측하지 않습니다. 확보된 위치·주소와 Google 지도 링크를 표시하며 사진/리뷰가 없다고 안내합니다.
- 모바일 일정은 시간 / 제목 / 짧은 설명을 한 줄로 표시하고 긴 글은 말줄임합니다. 상세 내용은 기존 ‘자세히’ 버튼으로 펼칩니다.

## 정보 범위와 검증

Google 지도 전체 웹페이지를 팝업 안에 복제하는 방식이 아닙니다. Places API가 제공하는 필드와 사진(최대 10개), 리뷰(최대 5개)를 표시하며 나머지는 원문 링크로 확인합니다. 정보 조회는 API 사용량에 포함됩니다.

[Google 장소 상세 공식 문서](https://developers.google.com/maps/documentation/javascript/place-details)

자동 테스트: 이동수단 순환, 점선·시간 생략·걷기, 속도, 저장 실패, 팝업 사진/리뷰/출처, 로딩 중 닫기, 빈 위치 안내를 확인했습니다. 실제 DB에서 OTHER 저장·조회·편집/열람 권한·날짜 변경 시 보존을 확인했고 테스트 데이터는 롤백했습니다. 실제 iPhone Safari 및 실제 Google 응답을 통한 전체 UI 검증은 하지 못했습니다.
