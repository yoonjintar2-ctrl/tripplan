# 여행계획닷컴 v16 수정본

## GitHub에 적용하기

이 ZIP은 현재 사이트 위에 덮어쓰는 업데이트입니다. 기존 캐릭터·캡틴 이미지가 들어 있는 `dist/assets` 폴더는 그대로 사용합니다.

1. ZIP을 압축 해제합니다.
2. GitHub `yoonjintar2-ctrl/tripplan` 저장소의 `main` 브랜치, 최상위 폴더에서 `Add file → Upload files`를 누릅니다.
3. 압축을 푼 폴더 안의 **dist, scripts, tests 폴더와 package.json**을 함께 끌어다 놓고 커밋합니다. ZIP 자체나 바깥쪽 delivery 폴더를 올리는 것이 아닙니다. 100개 미만의 파일로 구성되어 있습니다.
4. Actions의 배포가 성공하면 `https://trip.jintar.co.kr/`를 새 탭에서 열고 `Ctrl+Shift+R`로 새로고침합니다.
5. 여행 관리 목록에 **복제하기**가 있고, **동행별 일정·움직임 멈춤·실시간 따라가기 버튼**이 없어졌으면 새 버전입니다. HTML은 `app.js?v=16`, `avatar-studio.js?v=5`, `studio.css?v=13`을 사용합니다.

데이터베이스 SQL이나 Supabase 프로젝트 설정을 추가로 바꿀 필요는 없습니다. 현재 GitHub에 배포된 파일에는 이전에 전달한 지도 스타일·게스트 시작 보정 일부가 없어서, 이번 패키지는 최상위 화면 파일도 모두 포함합니다.

## 반영 내용

- 캐릭터 선택과 기본/노랑/갈색/흰색 머리색만 유지합니다. 의상·안경·모자·귀걸이 등 꾸미기와 위치 미세 조정을 제거했습니다. 기존에 저장한 소품 설정도 화면에 적용하지 않습니다.
- 머리색 경계에 연속적인 흐림 처리를 적용하고 피부색과의 전환을 부드럽게 조정했습니다.
- 첫 접속·로그인 확인 지연·기존 임시 계획 복원 상태에서도 게스트 일정 추가와 여행 관리가 가능합니다.
- 로그인 전환 중 일부 저장만 성공한 임시 계획은 기존 기록을 브라우저 내부에 보관하고 독립적인 임시 계획으로 이어서 편집할 수 있습니다. 이전에 일부 저장된 계정 여행이 있다면 남아 있을 수 있습니다.
- 계획 파일 백업 메뉴는 제거했습니다. 새로고침·로그인 이동 중 복구하기 위한 브라우저 임시 저장은 유지합니다.
- 여행 목록에서 복제하기를 제공합니다. 일정·날짜·장소·메모·정산 금액·동행을 복사하고 새 식별자를 부여합니다. 원본 공유 링크와 공동 편집 권한은 복사하지 않습니다.
- 로그인한 여행의 복제가 도중에 실패하면 같은 원본의 복제하기를 다시 눌러 이어서 저장합니다.
- 캡틴 산책의 수동 정지 버튼과 과거 정지 설정을 제거했습니다. 기기의 동작 줄이기 설정은 존중합니다.
- ON AIR 실시간 따라가기 토글을 제거했습니다. 현재 날짜·시간 기준으로 자동 추적합니다. 다른 일정은 잠깐 열어볼 수 있으며 다음 분 갱신 때 현재 일정으로 돌아갑니다.
- 동행별 일정 필터를 제거하고 일정 칸 안에 동행 얼굴을 최대 3명, 추가 인원은 +숫자로 표시합니다. 얼굴에 마우스를 올리면 전체 이름을 볼 수 있습니다.
- PC 장소 카드의 리뷰 영역 최대 높이를 105px에서 220px로, 펼친 카드 최대 높이를 350px에서 460px로 늘렸습니다. 작은 화면에서는 가용 높이에 맞춰 제한합니다.
- 메인 지도와 일정 편집 지도에 크림색 바탕·연한 녹색 공원·옅은 청록색 물의 파스텔 스타일을 적용했습니다.

## HTTPS와 로그인

확인 시점에는 `https://trip.jintar.co.kr/`가 인증서 오류 없이 정상 응답했고, 연결된 원격 브라우저에서도 접속했습니다. Enforce HTTPS 체크는 유지해 주세요. 이전 경고 화면을 열어 둔 탭은 닫고 HTTPS 주소로 새 탭에서 접속해 주세요.

브라우저의 HTTP/HTTPS 주소는 임시 저장 공간이 서로 다릅니다. 이전 HTTP 주소에 있던 임시 계획을 HTTPS로 자동 이전했다고 가정하지 마세요. 문제 확인 중에는 사이트 데이터나 쿠키를 삭제하지 마세요.

Supabase 로그인 복귀 기본 주소는 앞서 `https://trip.jintar.co.kr/`로 수정한 상태입니다. 이번 수정본에서 로그인 전 임시 보관과 로그인 후 계정 전송을 유지합니다.

## 확인 범위

- 기존 배포 사이트: HTTPS 접속, 로그인 없는 일정 추가 창 열기·일정 저장·여행 관리 창 열기를 원격 브라우저로 확인했습니다.
- 수정 코드: DOM 통합 테스트로 첫 클릭, 로그인 지연, 일정 수정·메모·정산·장소 선택, 새로고침 복원, 샘플 여행, OAuth 복귀/계정 전송, 중단된 전송 복구, 게스트·계정 여행 복제와 복제 재시도를 검증했습니다. Google Maps와 Supabase 응답은 테스트에서 모의 처리했습니다.
- 머리색: 실제 합성 코드로 대표 헤어 8종과 4가지 색을 렌더링해 확인했습니다.
- 수정본 배포 후 실제 Google 로그인과 새 지도 색상·PC/모바일 배치는 추가 확인이 필요합니다. 사용자 계정으로 실제 로그인·저장까지 완료했다고 주장하는 검증은 아닙니다.
