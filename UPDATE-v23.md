# 여행계획닷컴 v23 누적 업데이트

## 적용

ZIP을 풀고 내용물(dist, scripts, tests, supabase, package.json, 안내서)을 GitHub 저장소 최상위에 덮어쓰세요. ZIP 자체를 올리거나 dist 안에 dist를 만들면 안 됩니다. 기존 dist/assets 폴더, CNAME, 도메인과 GitHub Pages 설정은 유지합니다. 이번 ZIP의 새 캐릭터 파일 두 개도 dist/assets/studio에 함께 올려야 합니다.

v17~v22 코드 수정도 포함한 기존 사이트용 누적본입니다. 기존 이미지 전체를 포함한 새 설치본은 아닙니다.

현재 연결된 여행계획닷컴 Supabase 프로젝트에는 이동수단 저장용 컬럼을 적용했고 실제 DB 테스트도 통과했습니다. 같은 프로젝트를 사용한다면 별도 SQL 실행은 필요 없습니다. 다른 프로젝트로 옮길 때는 supabase/migrations/20260921152818_itinerary_transport_mode.sql을 적용합니다.

## 수정 내용

- 런웨이에서 캡틴이 직접 운전하는 크림색 컨버터블과 비행기 영상을 새로 제작했습니다. 차량 바퀴와 머리카락, 비행기 프로펠러와 기체가 움직입니다. 사이트에는 배경을 투명하게 만든 애니메이션 WebP로 적용했습니다.
- 빈 말풍선 때문에 캡틴 왼쪽 위에 보이던 점을 제거했습니다.
- 걷기 이미지의 정사각형 비율을 복원했습니다. 캐릭터를 세로로 늘리는 표시를 제거했습니다.
- 캡틴만 이동할 경로의 작은 굴곡을 정리하고, 같은 이동수단의 잦은 정지 구간을 합쳤습니다. 지도에 그려지는 실제 경로선은 유지합니다.
- 이동 애니메이션을 대략 0.8~2.2초 단위로 빠르게 조정하고 점프도 0.6초로 줄였습니다. 실제 소요시간은 버튼에 별도로 표시됩니다.
- 시간 버튼을 누르면 도보↔차량으로 전환합니다. 대중교통 버튼은 첫 클릭 시 도보로 전환합니다. 반대 경로를 먼저 조회하고 저장에 성공한 뒤 경로·시간·모션을 함께 바꿉니다.
- 반대 이동수단으로 경로를 찾을 수 없거나 저장에 실패하면 기존 경로를 유지하고 안내합니다.
- 선택한 이동수단은 도착 일정에 저장됩니다. 게스트 새로고침, Google 로그인 시 계정 이관, 여행 복제에도 유지됩니다. 순서를 바꾸면 해당 일정으로 들어오는 구간에 그 선호 이동수단이 적용됩니다.
- 비행기 첫 등장은 첫 목적지 위에서 내려오는 1.8초 연출입니다. 한국 출발 표시와 한국까지 확대되는 동작을 제거했습니다. 실제 항공편과 관계없는 오프닝입니다.
- 일자만 선택했을 때의 순회, 경로가 없을 때 점프, 지도 검색 등 기존 기능도 포함합니다.

## 실제 경로

Google Cloud의 기존 지도 프로젝트에서 Maps JavaScript API와 Routes API를 활성화해야 합니다. API 제한이 설정된 키라면 Routes API도 허용합니다. 결제 연결이 필요하며 경로 조회는 사용량에 포함됩니다.

[Google 공식 Routes 설정 안내](https://developers.google.com/maps/documentation/javascript/routes/start)

시간은 조회 시점의 Google 예상시간입니다. 경로 자체와 시간은 장기 저장하지 않고, 사용자가 고른 도보/차량 옵션만 저장하여 다시 조회합니다. 경로가 없거나 API가 응답하지 않으면 원래대로 선과 시간을 생략하고 점프합니다.

## 검증

- npm test 전체 통과.
- 도보 40분 강제 선택, 차량으로 재전환, 경로·시간·모션 변경, 저장 실패 복구, 선호 옵션 재로딩, 비행기 등장 시 지도 확대 방지: 모의 Google 응답으로 검증.
- 게스트 선택 저장·새로고침·계정 이관 검증.
- 실제 DB에서 편집자 저장, 열람자 수정 차단, 잘못된 이동수단 차단, 여행 날짜 변경 시 값 보존 테스트 통과. 테스트 데이터는 롤백했습니다.
- 차량·비행기 영상 프레임과 알파 투명도 확인.
- 실제 iPhone Safari와 실제 Google 경로 응답을 통한 전체 사용 흐름은 이번 환경에서 검증하지 못했습니다.
